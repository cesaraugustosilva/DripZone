# Brand Assets

Local brand logos for the home marquee should live here as reviewed SVG or transparent PNG files.

When a reliable asset is added, update `brands-assets.json` and `frontend/data/featured-brands.json`.
